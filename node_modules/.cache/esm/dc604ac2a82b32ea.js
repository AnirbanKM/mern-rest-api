let DEBUG_MODE,ValidationError,CustomErrorHandler;_a82‍.x([["default",()=>_a82‍.o]]);_a82‍.w("../config",[["DEBUG_MODE",["DEBUG_MODE"],function(v){DEBUG_MODE=v}]]);_a82‍.w("joi",[["ValidationError",["ValidationError"],function(v){ValidationError=v}]]);_a82‍.w("../services/CustomErrorHandler",[["default",["CustomErrorHandler"],function(v){CustomErrorHandler=v}]]);



const errorHandler = (err, req, res, next) => {

    console.log(`Getting error from next paramter`);
    _a82‍.g.console.log(err);

    let statusCode = 500;

    let data = {
        message: 'Intertal server error',
        ...(DEBUG_MODE === 'true' && { originalError: err.message })
    }

    if (err instanceof ValidationError) {
        statusCode = 422;
        data = {
            message: err.message
        }
    }

    if (err instanceof CustomErrorHandler) {
        statusCode = err.status;
        data = {
            message: err.message
        }
    }

    return res.status(statusCode).json(data);
}


_a82‍.d(errorHandler);